## 1. バックエンド: 会話クリアと履歴上限

- [x] 1.1 usecase `chat` に `ClearConversation` を追加し、単一スレッドの `event_proposal`→`message` をトランザクション内で依存順に削除する。ユニットテスト（既存の `chat_test.go` に準拠）でクリア後にメッセージ・予定案が空になることを検証する
- [x] 1.2 `handler/chat.go` にクリアハンドラを追加し、`router.go` の `/api/chat` に `DELETE /conversations` を登録する。ハンドラテストで 200/204 応答とクリア動作を検証する
- [x] 1.3 `send.go` の `buildHistory` を直近 N ターン（名前付き定数 `maxHistoryTurns = 10`）に切り詰める。上限超過時に末尾 N 件のみが返ることをテストで検証する（履歴 > N と ≤ N の両ケース）

## 2. フロントエンド API クライアント

- [x] 2.1 `api/client.ts` に `clearConversation()`（`DELETE /chat/conversations`）を追加し、`api/types.ts` に必要な型があれば追加する。型チェック（`tsc`/ビルド）が通ることを確認する

## 3. フロントエンド: チャット画面

- [x] 3.1 `ChatScreen.handleSend` を楽観的表示に変更（送信前に暫定ユーザーメッセージを append、`loading` 中は処理中表示、成功で `reload()` により全置換、失敗でロールバック＋入力復元）。実機/ブラウザで送信直後に自メッセージが即表示されることを確認する
- [x] 3.2 チャット画面ヘッダーに会話クリアボタンを追加し、押下時に確認ダイアログを表示、承諾時のみ `api.clearConversation()` を呼びローカル状態を空にする。確認キャンセルで消えないこと、承諾でクリアされることを確認する

## 4. フロントエンド: カレンダー画面

- [x] 4.1 RBC の `messages.previous`/`messages.next` を `'<'`/`'>'` に変更する（`today` は「今日」維持）。表示ラベルが変わることを確認する
- [x] 4.2 予定選択フローを「詳細（読み取り専用）→編集」に変更: 新規 `EventDetail` コンポーネント（モーダル、名称・日時・場所・概要・情報源＋「編集」「削除」「閉じる」）を追加し、`onSelectEvent` は `EventForm` ではなく詳細を開くようにする。状態を `viewing`/`editing` に分離する。詳細の「編集」で `EventForm`（編集モード）へ遷移すること、詳細の「削除」で確認のうえ削除できることを確認する
- [x] 4.3 見切れ修正: `.calendar-container`/Calendar 高さを決定的にし（D4）、必要に応じて期間切替時の再マウント `key` を付与する。主対象のデスクトップ狭幅で「今日」「前」「次」の各遷移後に月表示が見切れないことを確認し、モバイル幅（エミュレータ）でも保険として確認する

## 5. フロントエンド: ナビゲーション順序と初期表示

- [x] 5.1 `App.tsx` の初期 `tab` を `'calendar'` に変更し、`BottomTabs.TABS` の並びを `calendar → chat → settings` にする。既存の `BottomTabs.test.tsx` を更新/追加し、起動時カレンダー表示とタブ順序を検証する

## 6. スタイル: モバイル対応

- [x] 6.1 `.bottom-tabs` にセーフエリア考慮（`padding-bottom: env(safe-area-inset-bottom)` と高さの `calc`）を追加し、下部タブが端末ナビゲーションバーと重ならないことをセーフエリアを持つ端末/エミュレータで確認する
- [x] 6.2 モバイル幅メディアクエリで RBC agenda（一覧）テーブルをカード風の縦積みに再スタイルし、各項目が折り返しで潰れず読めることをモバイル幅で確認する

## 7. 総合検証

- [x] 7.1 バックエンドの全テスト（`go test ./...` 相当）とフロントのビルド/型チェック・既存テストが通ることを確認する
- [x] 7.2 モバイル幅で一連のフロー（起動→カレンダー表示、予定タップ→詳細→編集、月ナビゲーション、チャット送信の即時表示、会話クリア）を通し、proposal の各項目が解消されていることを確認する
